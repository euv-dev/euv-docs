mod doc_page;
mod home_page;
mod layout;
mod navbar;
mod not_found;
mod sidebar;
mod toc;

pub(crate) use {
    doc_page::*, home_page::*, layout::*, navbar::*, not_found::*, sidebar::*, toc::*,
};

use super::*;
